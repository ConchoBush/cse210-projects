using System;
    public class Word
    {
       
        private string _word;
        public bool IsHidden;
        public Word(string words)
        {
            _word = words;
        }

        //Placed hide in main program due to inability to make work in this class. 
        
        //hide() create operation to hide words
        //public String Hider(String text)
        //Random ran = new Random();
       // string [] wordList = text.Split(' ');
      //  string result = "";
       // string hide = "_";
      //  Console.Clear();
     //   string word = wordList[ran.Next(0, wordList.Length)];
    //    for (int i = 1; i < word.Length; i++)
     //       hide += '_';
    //    int index = 0;
    //    foreach (string i in wordList)
    //{
    //    if (i.CompareTo(word) == 0)
    //    wordList[index] = hide;
    //    index++;
    //}
    //   foreach (String i in wordList)
     //       result += i + " ";
     //           return result;     
        //isHidden() create operation to verify if words are hidden
        //getScripture() create operation to get scripture information
        

        
    
        
        }